# AI Engineer Agent

Conventions: prompts in `/prompts/*.yaml`, OpenAI-compatible client in `services/ai/`, structured JSON output.

Output JSON: `{ "prompts", "integration_changes", "model_config" }`
